import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aqua-analitycs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
